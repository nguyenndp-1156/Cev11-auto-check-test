import type { ChakraTheme } from '@chakra-ui/react';

const appFont = "'Roboto', sans-serif";

const fonts: ChakraTheme['fonts'] = {
  body: appFont,
  html: appFont,
  heading: appFont,
};

export default fonts;
